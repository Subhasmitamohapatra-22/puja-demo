import React from 'react'

const usestate = () => {
  return (
    <>
    function count(
        const[Count,setCount]=(1);
        setCount(count+1);
    );
   
    </>
  )
}

export default usestate
